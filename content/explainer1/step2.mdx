import { InlineMath } from 'react-katex'
export default function Step2() {
  return (
    <>
      <h2>Data distribution</h2>
      <p>
        Here we inspect sample points. As we scroll the scatter updates to reflect sampling and jitter.
      </p>
      <p>Example inline math: <InlineMath math={'y = mx + c'} /></p>
    </>
  )
}
