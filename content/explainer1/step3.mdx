import { BlockMath } from 'react-katex'
export default function Step3() {
  return (
    <>
      <h2>Comparing groups</h2>
      <BlockMath math={'L(w) = \\sum_i (y_i - \\hat{y}_i)^2'} />
      <p>
        The bar chart shows aggregated values (group counts, errors, etc.). Values change per step.
      </p>
    </>
  )
}
